# No se necesitan variables de entorno en el frontend.
# El archivo vercel.json maneja la redirección /api/* → backend en Render.
# Edita vercel.json y reemplaza TU-APP.onrender.com por tu URL real de Render.
